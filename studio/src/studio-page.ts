export const STUDIO_PAGE = `<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>Voicecan Studio</title>
  <style>
    :root{--ink:#17201d;--paper:#f4f1e8;--panel:#fffdf6;--signal:#39a96b;--warning:#e06b32;--steel:#748079;--line:#cbd0c8;--danger:#a43f35;--focus:#cce9d7;font-family:"IBM Plex Sans","Noto Sans SC",sans-serif;color:var(--ink);background:var(--paper);color-scheme:light}
    *{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;background:linear-gradient(90deg,#17201d 0 58px,var(--paper) 58px);height:100vh;overflow:hidden}button,input,select,textarea{font:inherit}button{cursor:pointer}.app{display:grid;grid-template-columns:58px minmax(280px,320px) minmax(0,1fr);height:100vh;min-height:0}.rail{position:sticky;top:0;height:100vh;color:#fff;display:flex;align-items:center;flex-direction:column;padding:16px 8px;gap:16px}.mark{width:38px;height:38px;border:1px solid #56615c;display:grid;place-items:center;font-family:"IBM Plex Mono",monospace;font-weight:700}.rail-line{width:1px;background:#47514d;flex:1}.rail-status{writing-mode:vertical-rl;font:12px "IBM Plex Mono",monospace;letter-spacing:.12em}.rail-dot{width:10px;height:10px;border-radius:50%;background:var(--warning);border:2px solid #fff}.rail-dot.ready{background:var(--signal)}
    .recordings{border-right:1px solid var(--line);background:#ebe7dc;height:100vh;min-height:0;overflow-y:auto;overscroll-behavior:contain;scrollbar-gutter:stable}.recordings-head{position:sticky;top:0;z-index:4;padding:22px 18px 15px;background:#ebe7dc;border-bottom:1px solid var(--line)}.kicker{font:700 10px "IBM Plex Mono",monospace;letter-spacing:.15em;text-transform:uppercase;color:#53605a}.recordings h1{font:700 28px/1 "IBM Plex Sans Condensed","Noto Sans SC",sans-serif;margin:8px 0 17px}.toolbar{display:grid;grid-template-columns:1fr auto;gap:8px}.toolbar input{grid-column:1/-1}.field,input,select,textarea{width:100%;border:1px solid #aeb6b0;background:var(--panel);color:var(--ink);padding:10px 11px;border-radius:3px}input:focus,select:focus,textarea:focus,button:focus-visible,a:focus-visible,summary:focus-visible{outline:3px solid var(--focus);outline-offset:1px;border-color:var(--signal)}.btn{display:inline-flex;align-items:center;justify-content:center;gap:6px;border:1px solid var(--ink);background:var(--ink);color:#fff;padding:9px 12px;border-radius:3px;text-decoration:none}.btn.secondary{background:transparent;color:var(--ink)}.btn.warning{background:var(--warning);border-color:var(--warning)}.btn.danger{background:var(--danger);border-color:var(--danger)}.btn:disabled{opacity:.45;cursor:not-allowed}.job-list{display:grid}.job{border:0;border-bottom:1px solid var(--line);background:transparent;text-align:left;padding:15px 18px;display:grid;gap:7px;color:inherit}.job:hover,.job.active{background:var(--panel)}.job.active{box-shadow:inset 4px 0 var(--signal)}.job-row{display:flex;justify-content:space-between;gap:10px;align-items:start}.job-title{font:650 15px "IBM Plex Sans Condensed","Noto Sans SC",sans-serif}.mono{font-family:"IBM Plex Mono",monospace}.muted{color:#59665f}.small{font-size:12px}.status{display:inline-flex;align-items:center;gap:6px;font:700 10px "IBM Plex Mono",monospace;letter-spacing:.05em;text-transform:uppercase}.status::before{content:"";width:7px;height:7px;border-radius:50%;background:var(--steel)}.status.completed::before,.status.delivered::before,.status.accepted::before,.status.submitted::before{background:var(--signal)}.status.failed::before,.status.stale::before,.status.unknown::before{background:var(--warning)}
    .workspace{height:100vh;min-width:0;min-height:0;overflow-y:auto;overscroll-behavior:contain;scrollbar-gutter:stable;padding:26px clamp(22px,3vw,42px) 90px}.workspace-head{display:flex;align-items:start;justify-content:space-between;gap:24px;margin-bottom:20px}.workspace h2{font:700 clamp(30px,3.2vw,42px)/1.03 "IBM Plex Sans Condensed","Noto Sans SC",sans-serif;margin:5px 0}.head-actions{display:flex;gap:8px;flex-wrap:wrap;justify-content:flex-end}.token-panel{position:fixed;z-index:9;right:30px;top:82px;width:min(420px,calc(100vw - 28px));padding:14px;background:var(--panel);border:1px solid var(--ink);box-shadow:8px 8px 0 #b9c0ba}.token-panel label{display:grid;gap:6px}.notice{position:fixed;right:24px;bottom:24px;z-index:12;max-width:min(460px,calc(100vw - 32px));padding:11px 14px;background:#e5f4e9;border:1px solid #b7d9c1;border-left:4px solid var(--signal);box-shadow:0 8px 24px rgba(23,32,29,.16)}.notice.error{background:#f7e7e2;border-color:#d9aaa3;border-left-color:var(--danger)}.hidden{display:none!important}.empty{padding:52px 20px;text-align:center;color:var(--steel)}
    .pipeline{position:sticky;top:10px;z-index:5;display:grid;grid-template-columns:repeat(5,1fr);border:1px solid var(--line);background:rgba(255,253,246,.96);margin-bottom:26px;box-shadow:0 5px 18px rgba(23,32,29,.06);backdrop-filter:blur(8px)}.stage-chip{padding:11px 13px;border-right:1px solid var(--line);display:grid;gap:3px;color:inherit;text-decoration:none}.stage-chip:last-child{border-right:0}.stage-chip:hover{background:#f0eee5}.stage-chip b{font:700 11px "IBM Plex Mono",monospace}.stage-chip span{font-size:13px}.stage-chip.done{box-shadow:inset 0 3px var(--signal)}.stage-chip.blocked{box-shadow:inset 0 3px var(--warning)}
    .stage{scroll-margin-top:92px;border-top:1px solid var(--ink);padding:20px 0 34px}.stage-label{display:grid;grid-template-columns:58px auto minmax(180px,1fr);align-items:end;column-gap:14px;margin-bottom:17px}.stage-number{grid-row:1/3;font:700 38px/1 "IBM Plex Mono",monospace;color:#bec5c0}.stage-label h3{font:700 22px "IBM Plex Sans Condensed","Noto Sans SC",sans-serif;margin:0}.stage-label .muted{text-align:right;padding-bottom:2px}.stage-body{min-width:0}.meta-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));border:1px solid var(--line);background:var(--panel);margin-bottom:14px}.meta-grid div{padding:11px 13px;border-right:1px solid var(--line)}.meta-grid div:last-child{border-right:0}.meta-grid b{display:block;margin-top:4px}.audio{width:100%}.segments{display:grid;gap:1px;background:var(--line);border:1px solid var(--line)}.segment{background:var(--panel);padding:11px 13px;display:grid;grid-template-columns:88px minmax(0,1fr);gap:12px}.segment.seekable{cursor:pointer}.segment.seekable:hover{background:#f7f5ed}.segment.active{background:#eef6f0;box-shadow:inset 4px 0 var(--signal)}.segment-time{font:12px "IBM Plex Mono",monospace;color:var(--steel);border:0;background:transparent;padding:0;text-align:left;align-self:start}.segment-time:disabled{opacity:1;cursor:default}.segment textarea{min-height:58px;resize:vertical;cursor:text}.summary-grid{display:grid;grid-template-columns:minmax(0,1fr);gap:14px}.summary-copy,.editor,.delivery-box,.review-panel,.context-card{background:var(--panel);border:1px solid var(--line);padding:18px}.summary-copy h4{font:700 19px "IBM Plex Sans Condensed","Noto Sans SC",sans-serif;margin:18px 0 6px}.summary-copy h4:first-child{margin-top:0}.summary-copy ul{padding-left:20px}.editor textarea{min-height:360px;font:12px/1.55 "IBM Plex Mono",monospace}.review-grid{display:grid;grid-template-columns:minmax(0,1.35fr) minmax(260px,.65fr);gap:14px}.review-panel{align-self:start}.review-panel h4,.context-card h4{margin:0 0 8px;font-size:16px}.review-panel label,.field-label{display:grid;gap:6px;font-weight:650;margin-top:12px}.review-state{display:flex;align-items:center;justify-content:space-between;gap:10px;padding-bottom:12px;border-bottom:1px solid var(--line)}.scenario-toolbar{display:grid;grid-template-columns:auto minmax(0,1fr);align-items:center;gap:12px;margin-bottom:7px}.scenario-toolbar label{font-weight:700}.scenario-purpose{margin:0 0 14px;padding-left:93px}.scenario-sections{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:10px;margin-top:17px}.scenario-section{border-top:3px solid var(--signal);background:#f2f4ed;padding:12px 13px}.scenario-section h5{margin:0 0 8px;font-size:14px}.scenario-section ul{margin:0;padding-left:18px}.advanced{margin-top:14px;border:1px solid var(--line);background:#eeece4}.advanced>summary{cursor:pointer;padding:12px 15px;font-weight:650}.advanced[open]>summary{border-bottom:1px solid var(--line)}.advanced .editor{border:0;background:var(--panel)}.advanced .editor textarea{min-height:330px}.actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:13px}.barrier{padding:12px 14px;border-left:4px solid var(--warning);background:#f7e9df;margin-bottom:14px}.action-grid{display:grid;grid-template-columns:minmax(250px,.72fr) minmax(0,1.28fr);gap:14px}.action-config{background:var(--panel);border:1px solid var(--line);padding:18px;align-self:start}.action-config h4,.action-history h4{margin:0 0 10px}.action-config label{display:grid;gap:5px;margin-top:10px;font-weight:650}.help{font-size:12px;color:#59665f;font-weight:400}.courier-guide{margin-bottom:14px;border:1px solid var(--line);background:#eef4ef}.courier-guide>summary{cursor:pointer;padding:12px 14px;font-weight:700}.courier-guide[open]>summary{border-bottom:1px solid var(--line)}.courier-guide ol{margin:12px 18px 14px;padding-left:20px}.courier-guide li+li{margin-top:6px}.action-history{min-width:0}.delivery-list{display:grid;gap:10px}.delivery{background:var(--panel);border:1px solid var(--line);padding:13px}.delivery .actions{margin-top:10px}.delivery details{margin-top:10px}.delivery pre{white-space:pre-wrap;overflow-wrap:anywhere}.setup{max-width:920px}.setup-grid{display:grid;grid-template-columns:1fr 1fr;gap:14px}.setup-grid .wide{grid-column:1/-1}.setup label{display:grid;gap:6px;font-weight:650}.setup section{border-top:1px solid var(--line);padding-top:18px;margin-top:20px}.toggle{display:flex!important;grid-column:1/-1;flex-direction:row;align-items:center;gap:9px}.toggle input{width:auto}.icon{width:17px;height:17px;vertical-align:-3px;fill:none;stroke:currentColor;stroke-width:2}
    @media(max-width:1080px){.app{grid-template-columns:52px 286px minmax(0,1fr)}body{background:linear-gradient(90deg,#17201d 0 52px,var(--paper) 52px)}.rail{padding-inline:6px}.workspace{padding-inline:20px}.review-grid,.action-grid{grid-template-columns:1fr}.meta-grid{grid-template-columns:1fr 1fr}.meta-grid div:nth-child(2){border-right:0}.head-actions .btn{padding-inline:9px}}
    @media(max-width:760px){body{height:auto;min-height:100vh;overflow:auto;background:var(--paper)}.app{display:block;height:auto;min-height:100vh}.rail{position:static;width:100%;height:44px;flex-direction:row;background:var(--ink);padding:5px 12px}.mark{width:34px;height:34px}.rail-line{height:1px;width:auto}.rail-status{writing-mode:horizontal-tb}.recordings{height:auto;min-height:auto;overflow:visible;border-right:0;border-bottom:1px solid var(--line)}.recordings-head{position:static;padding:13px 14px 11px}.recordings-head .kicker{display:none}.recordings h1{font-size:22px;margin:0 0 10px}.toolbar{grid-template-columns:minmax(0,1fr) auto}.toolbar input{grid-column:auto}.toolbar select{display:none}.toolbar .btn{white-space:nowrap;padding-inline:10px}.recordings-head #sync-meta{display:none}.job-list{display:flex;max-height:none;overflow-x:auto;scroll-snap-type:x mandatory}.job{min-width:285px;max-width:85vw;padding:11px 14px;border-right:1px solid var(--line);scroll-snap-align:start}.workspace{height:auto;min-height:0;overflow:visible;padding:17px 14px 60px}.workspace-head{display:grid;gap:10px;margin-bottom:14px}.workspace-head .kicker{display:none}.workspace h2{font-size:28px;margin:0}.workspace-head .mono{font-size:11px;overflow-wrap:anywhere}.head-actions{justify-content:flex-start}.head-actions .btn{padding:7px 10px;font-size:13px}.pipeline{top:6px;display:flex;overflow-x:auto;margin:0 -1px 20px;box-shadow:0 4px 12px rgba(23,32,29,.08)}.stage-chip{min-width:108px;padding:9px 10px;flex:1 0 auto}.stage{scroll-margin-top:74px;padding:17px 0 28px}.stage-label{grid-template-columns:42px 1fr;column-gap:8px;margin-bottom:13px}.stage-number{font-size:31px}.stage-label h3{font-size:19px}.stage-label .muted{grid-column:2;text-align:left;padding:0}.meta-grid{font-size:13px}.meta-grid div{padding:9px}.segment{grid-template-columns:1fr;padding:10px}.segment-time{display:flex;gap:8px}.scenario-toolbar{grid-template-columns:1fr}.review-grid,.action-grid{grid-template-columns:1fr}.summary-copy,.editor,.delivery-box,.review-panel,.context-card,.action-config{padding:14px}.advanced .editor textarea{min-height:270px}.setup-grid{grid-template-columns:1fr}.setup-grid .wide{grid-column:auto}.token-panel{right:14px;top:56px}}
  </style>
</head>
<body>
<svg aria-hidden="true" class="hidden"><symbol id="i-sync" viewBox="0 0 24 24"><path d="M20 7h-5V2M4 17h5v5M5.2 9A7 7 0 0 1 17 5l3 2M18.8 15A7 7 0 0 1 7 19l-3-2"/></symbol><symbol id="i-settings" viewBox="0 0 24 24"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.7 1.7 0 0 0 .3 1.9l.1.1-2.8 2.8-.1-.1a1.7 1.7 0 0 0-1.9-.3 1.7 1.7 0 0 0-1 1.6v.2h-4V21a1.7 1.7 0 0 0-1-1.6 1.7 1.7 0 0 0-1.9.3l-.1.1L4.2 17l.1-.1a1.7 1.7 0 0 0 .3-1.9A1.7 1.7 0 0 0 3 14H2.8v-4H3a1.7 1.7 0 0 0 1.6-1 1.7 1.7 0 0 0-.3-1.9L4.2 7 7 4.2l.1.1A1.7 1.7 0 0 0 9 4.6a1.7 1.7 0 0 0 1-1.6v-.2h4V3a1.7 1.7 0 0 0 1 1.6 1.7 1.7 0 0 0 1.9-.3l.1-.1L19.8 7l-.1.1a1.7 1.7 0 0 0-.3 1.9 1.7 1.7 0 0 0 1.6 1h.2v4H21a1.7 1.7 0 0 0-1.6 1Z"/></symbol></svg>
<main class="app">
  <aside class="rail"><div class="mark">VC</div><div class="rail-line"></div><div id="rail-status" class="rail-status">检查中</div><div id="rail-dot" class="rail-dot"></div></aside>
  <aside class="recordings"><div class="recordings-head"><div class="kicker">已授权录音</div><h1>Voicecan Studio</h1><div class="toolbar"><input id="search" type="search" aria-label="搜索录音" placeholder="搜索录音、场景或内容"><select id="filter" aria-label="筛选状态"><option value="">全部状态</option><option value="attention">需要处理</option><option value="completed">已完成</option><option value="processing">处理中</option></select><button id="sync" class="btn" type="button"><svg class="icon"><use href="#i-sync"/></svg> 刷新来源</button></div><div id="sync-meta" class="small muted" style="margin-top:10px">读取运行状态…</div></div><div id="jobs" class="job-list"></div></aside>
  <section class="workspace"><div id="notice" class="notice hidden" role="status"></div><div class="workspace-head"><div><div class="kicker">来源 → 处理 → 场景 → 审核 → 动作</div><h2 id="workspace-title">选择一条录音</h2><div id="workspace-sub" class="mono small muted">Device Platform 提供来源，Studio 完成应用闭环</div></div><div class="head-actions"><button class="btn secondary" id="token" type="button">操作令牌</button><button class="btn secondary" id="setup-open" type="button"><svg class="icon"><use href="#i-settings"/></svg> 设置 / 诊断</button></div></div><div id="token-panel" class="token-panel hidden"><label>操作令牌<input id="token-value" type="password" autocomplete="off" placeholder="只保存在当前浏览器会话"></label><div class="actions"><button id="token-save" class="btn" type="button">保存到当前会话</button><button id="token-clear" class="btn secondary" type="button">清除</button></div></div><div id="workspace"><div class="empty">从左侧选择录音，或先完成设置。</div></div></section>
</main>
<script>
const esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
const icon=name=>'<svg class="icon" aria-hidden="true"><use href="#i-'+name+'"/></svg>';
let jobs=[],scenarios=[],runtime=null,selected=null,editing=false,playing=false,timer=null,noticeTimer=null,renderedDetailKey=null;const playbackPositions=new Map(),summaryRequests=new Set(),summaryRequestErrors=new Map();
const operatorHeaders=()=>{const token=sessionStorage.getItem('studio.operatorToken');return token?{authorization:'Bearer '+token}:{}};
async function api(path,options={}){const response=await fetch(path,{...options,headers:{...operatorHeaders(),...(options.headers||{})}});const type=response.headers.get('content-type')||'';const body=response.status===204?null:type.includes('json')?await response.json():await response.text();if(!response.ok)throw new Error(body?.message||body?.error||String(body)||'请求失败');return body}
function notify(message,error=false,duration=error?9000:4200){const labels={'Transcript Revision 已保存':'转写内容已保存','Processor 与 Scenario 已重新生成':'处理完成，应用视图已更新','Processor Artifact 已修订':'处理结果已保存','Scenario Revision 已保存':'场景结果已保存','Scenario Revision 已确认':'当前结果已确认','Scenario 确认已撤销':'已撤销确认','Provider 状态已同步':'Courier 状态已更新','Delivery 已取消':'发送已取消'};const node=document.querySelector('#notice');if(noticeTimer)clearTimeout(noticeTimer);node.textContent=labels[message]||message;node.classList.toggle('hidden',!message);node.classList.toggle('error',error);if(message&&duration>0)noticeTimer=setTimeout(()=>{node.classList.add('hidden');noticeTimer=null},duration)}
function formatDate(value){if(!value)return'时间未知';return new Intl.DateTimeFormat('zh-CN',{month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',hour12:false}).format(new Date(value))}
function clock(ms){const seconds=Math.floor(Number(ms||0)/1000);return String(Math.floor(seconds/60)).padStart(2,'0')+':'+String(seconds%60).padStart(2,'0')}
const STATE_LABELS={received:'已接收',downloading:'下载中',processing:'处理中',post_processing:'后处理中',completed:'已完成',needs_attention:'需要处理',attention:'需要处理',failed:'失败',canceled:'已取消',not_requested:'未请求',queued:'排队中',stale:'已失效',skipped:'已跳过',waiting:'等待中',required:'待确认',confirmed:'已确认',draft:'草稿',executing:'执行中',submitted:'已提交',pending:'等待提交',accepted:'已受理',delivered:'已送达',unknown:'状态未知'};
const PRIORITY_LABELS={low:'低',medium:'中',high:'高'};
const VALUE_LABELS={'task-note':'任务笔记','decision-note':'决策笔记',memo:'备忘',low:'低',medium:'中',high:'高'};
const SCENARIO_GUIDES={'voice-inbox':'适合随手记和个人备忘，重点整理要点、标签与待办。','field-report':'适合巡检、维修和现场记录，重点提取设备、风险、发现与后续处理。','meeting-interview':'适合会议和访谈，重点整理议题、决策、责任人与截止时间。'};
function stateLabel(value){return STATE_LABELS[value]||value||'未知'}
function status(value){return'<span class="status '+esc(value)+'">'+esc(stateLabel(value))+'</span>'}
function scenarioTitle(id){return scenarios.find(item=>item.id===id)?.title||id}
function fieldLabel(scenarioId,key){return scenarios.find(item=>item.id===scenarioId)?.fields?.find(item=>item.key===key)?.label||key}
function displayValue(value){if(Array.isArray(value))return value.map(displayValue).join('、');if(typeof value==='boolean')return value?'是':'否';return VALUE_LABELS[value]||value}
function recordingTime(job){const timing=job.recording.timing||{};if(timing.device_started_at)return{value:timing.device_started_at,label:''};if(timing.device_ended_at&&Number.isFinite(Number(timing.duration_ms))){const inferred=new Date(timing.device_ended_at).getTime()-Number(timing.duration_ms);if(Number.isFinite(inferred))return{value:new Date(inferred).toISOString(),label:'推算时间'}}if(timing.synced_at)return{value:timing.synced_at,label:'同步时间'};if(timing.discovered_at)return{value:timing.discovered_at,label:'发现时间'};return{value:job.created_at,label:'导入时间'}}
function jobDate(job){return recordingTime(job).value}
function needsAttention(job){return ['failed','needs_attention'].includes(job.state)||job.summary_state==='failed'||job.summary_state==='stale'||job.scenario_state==='stale'||job.action_intents.some(item=>item.state==='failed')||job.deliveries.some(item=>['failed','unknown'].includes(item.state))}
function detailKey(job){return[job.updated_at,job.state,job.summary_state,job.summary_revision,job.scenario_id,job.scenario_state,job.scenario_revision,job.scenario_confirmation?.scenario_revision,(job.action_intents||[]).map(item=>item.id+':'+item.state).join(','),(job.deliveries||[]).map(item=>item.id+':'+item.state).join(',')].join('|')}
function isInteracting(){const active=document.activeElement;return editing||playing||Boolean(active&&active.matches('input,select,textarea'))||Boolean(document.querySelector('#workspace details[open]'))}
async function load({forceDetail=false}={}){runtime=await api('/api/v1/runtime');document.querySelector('#rail-status').textContent=runtime.configured?'就绪':'待配置';document.querySelector('#rail-dot').classList.toggle('ready',runtime.configured);const sync=runtime.sync||{};document.querySelector('#sync-meta').textContent=sync.running?'正在刷新授权来源':sync.completed_at?'最近刷新 '+formatDate(sync.completed_at)+' · 新增 '+(sync.created||0):'尚未刷新';if(!runtime.configured){renderSetup();return}[jobs,scenarios]=await Promise.all([api('/api/v1/recordings'),api('/api/v1/scenarios')]);jobs.sort((a,b)=>String(jobDate(b)).localeCompare(String(jobDate(a))));renderJobs();const current=jobs.find(item=>item.id===selected);if(current&&(forceDetail||!isInteracting())&&(forceDetail||renderedDetailKey!==detailKey(current)))await show(selected)}
function renderJobs(){const query=document.querySelector('#search').value.trim().toLowerCase(),filter=document.querySelector('#filter').value;const visible=jobs.filter(job=>{const hay=[job.recording_id,job.transcript?.text,job.scenario_id,job.scenario_result?.title].join(' ').toLowerCase();const matchFilter=!filter||filter==='attention'&&needsAttention(job)||filter==='completed'&&job.state==='completed'||filter==='processing'&&job.state!=='completed';return hay.includes(query)&&matchFilter});document.querySelector('#jobs').innerHTML=visible.map(job=>{const time=recordingTime(job),timeSource=time.label||'设备录音时间';return'<button class="job '+(selected===job.id?'active':'')+'" data-id="'+esc(job.id)+'" type="button"><div class="job-row"><span class="job-title">'+esc(job.scenario_result?.title||'待整理录音')+'</span>'+status(needsAttention(job)?'attention':job.state)+'</div><div class="small mono muted">'+esc(timeSource)+' · '+esc(formatDate(time.value))+'</div><div class="small muted">'+esc((job.scenario_result?.overview||job.transcript?.text||job.recording_id).slice(0,76))+'</div><div class="job-row small"><span>'+esc(scenarioTitle(job.scenario_id))+'</span><span>转写版本 '+job.transcript_revision+' · 场景版本 '+job.scenario_revision+'</span></div></button>'}).join('')||'<div class="empty">没有匹配录音</div>';document.querySelectorAll('.job').forEach(node=>node.onclick=()=>show(node.dataset.id))}
function pipeline(job){const transcript=job.transcript?'done':job.state==='failed'?'blocked':'';const process=['completed','skipped'].includes(job.summary_state)?'done':['failed','stale'].includes(job.summary_state)?'blocked':'';const scenario=job.scenario_state==='completed'?'done':job.scenario_state==='stale'?'blocked':'';const review=job.scenario_confirmation?'done':'';const action=job.action_intents.some(item=>item.state==='completed')?'done':job.action_intents.some(item=>item.state==='failed')?'blocked':'';return'<nav class="pipeline" aria-label="处理流程"><a href="#stage-source" class="stage-chip '+transcript+'"><b>01 来源</b><span>'+esc(stateLabel(job.state))+'</span></a><a href="#stage-process" class="stage-chip '+process+'"><b>02 处理</b><span>'+esc(stateLabel(job.summary_state))+'</span></a><a href="#stage-scenario" class="stage-chip '+scenario+'"><b>03 场景</b><span>'+esc(stateLabel(job.scenario_state))+'</span></a><a href="#stage-scenario" class="stage-chip '+review+'"><b>04 审核</b><span>'+esc(stateLabel(job.scenario_confirmation?'confirmed':'required'))+'</span></a><a href="#stage-action" class="stage-chip '+action+'"><b>05 动作</b><span>'+job.action_intents.length+' 个动作</span></a></nav>'}
function transcriptStage(job){const audio=job.playback_ready?'<audio id="audio" class="audio" controls preload="metadata" src="/api/jobs/'+encodeURIComponent(job.id)+'/audio"></audio>':'<div class="barrier">当前录音没有可播放的本地预览，仍可手动补充转写内容。</div>';const hasText=Boolean(job.transcript?.segments?.some(segment=>segment.text.trim()));if(!hasText){const initial=job.transcript?.text?.trim()||'';return audio+'<div class="context-card"><div class="kicker">手动补充</div><h4>输入完整转写</h4><p class="small muted">没有识别到有效文字。你可以在这里输入或粘贴内容，保存后再运行处理。</p><label class="field-label" for="manual-transcript">转写内容<textarea id="manual-transcript" rows="8" placeholder="在这里输入录音中的文字…">'+esc(initial)+'</textarea></label><div class="actions"><button id="save-transcript" class="btn" type="button">保存转写内容</button></div></div>'}const rows=job.transcript.segments.map((segment,index)=>'<div class="segment '+(job.playback_ready?'seekable':'')+'" data-start="'+segment.start_ms+'" data-end="'+segment.end_ms+'" '+(job.playback_ready?'data-seek="'+segment.start_ms+'" title="点击跳转到此片段并播放"':'')+'><button class="segment-time" data-seek="'+segment.start_ms+'" type="button" '+(job.playback_ready?'':'disabled')+' aria-label="播放第 '+(index+1)+' 个片段">'+clock(segment.start_ms)+'–'+clock(segment.end_ms)+'<br>'+esc(segment.speaker||'说话人待识别')+'</button><textarea data-index="'+index+'" aria-label="第 '+(index+1)+' 个转写片段">'+esc(segment.text)+'</textarea></div>').join('');return audio+'<div class="segments">'+rows+'</div><div class="actions"><button id="save-transcript" class="btn" type="button">保存转写修订</button><a class="btn secondary" href="/api/jobs/'+encodeURIComponent(job.id)+'/export?format=srt">导出 SRT</a><a class="btn secondary" href="/api/jobs/'+encodeURIComponent(job.id)+'/export?format=json">导出 JSON</a></div>'}
function summaryCopy(summary){if(!summary)return'<div class="empty">尚未生成处理结果</div>';return'<div class="summary-copy"><h4>'+esc(summary.title)+'</h4><p>'+esc(summary.overview)+'</p><h4>议题</h4>'+summary.topics.map(item=>'<p><b>'+esc(item.title)+'</b><br>'+esc(item.summary)+'<br><span class="small mono muted">原文片段 '+esc(item.segment_refs.join(', '))+'</span></p>').join('')+'<h4>决策</h4><ul>'+summary.decisions.map(item=>'<li>'+esc(item.text)+'</li>').join('')+'</ul><h4>行动项</h4><ul>'+summary.action_items.map(item=>'<li>'+esc(item.text)+' · '+esc(item.assignee||'待确认')+'</li>').join('')+'</ul></div>'}
function summaryStage(job){const skipped=job.summary_state==='skipped';const processing=summaryRequests.has(job.id)||['queued','processing'].includes(job.summary_state);const requestError=summaryRequestErrors.get(job.id);const stale=job.summary_state==='stale'?'<div class="barrier">转写内容已经变化，请重新运行处理后再审核场景结果。</div>':'';const skipNotice=skipped?'<div class="barrier"><b>已跳过处理</b><br><span class="small">转写没有有效文字，不会调用摘要模型。编辑并保存转写后可以重新运行。</span></div>':'';const failed=job.summary_state==='failed'||requestError?'<div class="barrier"><b>处理失败</b><br><span class="small">'+esc(requestError||job.summary_error_message||job.summary_error_code||'摘要处理服务暂时不可用，请检查设置与运行日志后重试。')+'</span></div>':'';const advanced=job.summary?'<details class="advanced"><summary>高级：查看或编辑处理结果 JSON</summary><div class="editor"><label for="summary-json"><b>结果版本 '+job.summary_revision+'</b></label><textarea id="summary-json">'+esc(JSON.stringify(job.summary,null,2))+'</textarea><div class="actions"><button id="save-summary" class="btn" type="button" '+(job.summary_state==='stale'?'disabled':'')+'>保存修改</button></div></div></details>':'';const buttonLabel=processing?'处理中…':skipped?'无需处理':job.summary?'重新运行处理':'运行处理';return stale+skipNotice+failed+'<div class="review-grid">'+summaryCopy(job.summary)+'<aside class="context-card"><div class="kicker">处理状态</div><h4>根据当前转写生成</h4><p class="small muted">修改转写后，需要重新运行处理；场景结果会随之更新。</p>'+(job.summary?'<p class="small">当前为结果版本 '+job.summary_revision+'，使用转写版本 '+job.transcript_revision+'。</p>':'')+(processing?'<p class="small">本地模型正在生成结果，首次加载模型可能需要较长时间。</p>':'')+'<div class="actions"><button id="generate-summary" class="btn" type="button" '+(processing||skipped?'disabled '+(processing?'aria-busy="true"':''):'')+'>'+buttonLabel+'</button></div></aside></div>'+advanced}
function scenarioStage(job){const options=scenarios.map(item=>'<option value="'+esc(item.id)+'" '+(item.id===job.scenario_id?'selected':'')+'>'+esc(item.title)+' · '+esc(item.description)+'</option>').join('');const manifest=scenarios.find(item=>item.id===job.scenario_id);const purpose=SCENARIO_GUIDES[job.scenario_id]||manifest?.description||'';const result=job.scenario_result;if(!result)return'<div class="scenario-toolbar"><label for="scenario-select">应用场景</label><select id="scenario-select">'+options+'</select></div><p class="scenario-purpose small muted">'+esc(purpose)+'</p><div class="barrier">处理完成后，这里会按所选场景整理可审核的结果。</div>';const reviewStatus=job.scenario_confirmation?status('confirmed')+'<span class="small muted">当前结果已确认</span>':status('required')+'<span class="small muted">请检查内容后确认</span>';const sections=(result.sections||[]).map(section=>'<div class="scenario-section"><h5>'+esc(section.title)+'</h5>'+(section.items?.length?'<ul>'+section.items.map(item=>'<li>'+esc(item.text)+'</li>').join('')+'</ul>':'<div class="small muted">没有识别到相关内容</div>')+'</div>').join('');const values=Object.entries(result.values||{}).map(([key,value])=>'<li><b>'+esc(fieldLabel(job.scenario_id,key))+'</b> · '+esc(displayValue(value))+'</li>').join('')||'<li class="muted">没有额外字段</li>';const actions=(result.actions||[]).map(item=>'<li>'+esc(item.title)+' · '+esc(PRIORITY_LABELS[item.priority]||item.priority)+'优先级</li>').join('')||'<li class="muted">没有需要跟进的动作</li>';return'<div class="scenario-toolbar"><label for="scenario-select">应用场景</label><select id="scenario-select">'+options+'</select></div><p class="scenario-purpose small muted">'+esc(purpose)+'</p><div class="review-grid"><div class="summary-copy"><div class="kicker">'+esc(manifest?.title||'场景结果')+'</div><h4>'+esc(result.title)+'</h4><p>'+esc(result.overview)+'</p><div class="scenario-sections">'+sections+'</div><h4>本场景提取的信息</h4><ul>'+values+'</ul><h4>建议跟进</h4><ul>'+actions+'</ul></div><aside class="review-panel"><div class="review-state"><b>人工审核</b><span>'+reviewStatus+'</span></div><p class="small muted">确认后才能创建 Courier 发送预览，避免未经检查的内容被外发。</p><label for="review-actor">确认人<input id="review-actor" value="'+esc(job.scenario_confirmation?.confirmed_by||'studio-ui')+'"></label><label for="review-note">备注<input id="review-note" value="'+esc(job.scenario_confirmation?.note||'')+'" placeholder="可选"></label><div class="actions"><button id="confirm-scenario" class="btn" type="button">'+(job.scenario_confirmation?'再次确认当前结果':'确认当前结果')+'</button>'+(job.scenario_confirmation?'<button id="unconfirm-scenario" class="btn secondary" type="button">撤销确认</button>':'')+'<a class="btn secondary" href="/api/v1/recordings/'+encodeURIComponent(job.id)+'/scenario/export.md">导出 Markdown</a></div></aside></div><details class="advanced"><summary>高级：查看或编辑场景结果 JSON</summary><div class="editor"><label for="scenario-json"><b>场景结果版本 '+job.scenario_revision+'</b></label><textarea id="scenario-json">'+esc(JSON.stringify(result,null,2))+'</textarea><div class="actions"><button id="save-scenario" class="btn" type="button">保存修改</button></div></div></details>'}
function actionStage(job){const setupHint=runtime?.profile==='local-full'?'在 studio/.env 中设置 NOTIFICATION_ENABLED=true 和 COURIER_API_KEY，然后重启 Studio。':'在“设置 / 诊断”中启用 Courier，并填写 API 密钥。';const guide='<details class="courier-guide"><summary>第一次使用 Courier？查看接入步骤</summary><ol class="small"><li>'+esc(setupHint)+'</li><li>在 Courier Dashboard 创建消息模板，复制模板 ID。</li><li>准备接收人的 Courier User ID；渠道与用户偏好在 Courier 中配置。</li><li>回到这里生成预览，检查无误后再点击“确认发送”。</li></ol></details>';if(!job.scenario_confirmation)return guide+'<div class="barrier">请先在上一步检查并确认当前场景结果，然后才能创建发送预览。</div>';const actions=job.action_intents.map(item=>'<div class="delivery"><div class="job-row">'+status(item.state)+'<span class="small">场景版本 '+item.scenario_revision+'</span></div><div style="margin-top:8px"><b>'+esc(item.preview.title)+'</b></div><div class="small muted">'+esc(item.target.name)+' · Courier 模板 '+esc(item.target.workflow_id)+'</div><details><summary class="small">查看发送预览</summary><pre class="editor mono small">'+esc(JSON.stringify(item.preview,null,2))+'</pre></details><div class="actions">'+(item.state==='draft'?'<button class="btn warning execute-action" data-id="'+esc(item.id)+'" type="button">确认发送</button>':'')+'</div></div>').join('');const deliveries=job.deliveries.filter(item=>item.scenario_revision>0).map(item=>'<div class="delivery"><div class="job-row">'+status(item.state)+'<span class="mono small">'+esc(item.provider_request_id||'等待 Courier 返回')+'</span></div><div class="small muted" style="margin-top:8px">Courier · 第 '+item.attempt+' 次尝试</div><div class="actions"><button class="btn secondary refresh-delivery" data-id="'+esc(item.id)+'" type="button">刷新状态</button>'+(!['delivered','failed','canceled'].includes(item.state)?'<button class="btn secondary cancel-delivery" data-id="'+esc(item.id)+'" type="button">取消发送</button>':'')+'</div></div>').join('');const history=actions+deliveries||'<div class="empty context-card">尚未创建发送动作。填写接收人和模板后，先生成预览。</div>';return guide+'<div class="barrier">Studio 会把已确认的场景结果交给 Courier；实际渠道、用户偏好和重试策略由 Courier 管理。</div><div class="action-grid"><div class="action-config"><div class="kicker">新建发送</div><h4>创建 Courier 发送预览</h4><label for="target-name">接收目标名称<input id="target-name" value="运营团队"><span class="help">仅用于在 Studio 中辨认这次发送。</span></label><label for="target-user">Courier User ID<input id="target-user" required placeholder="例如 user_123"><span class="help">接收人在 Courier 中的用户标识。</span></label><label for="target-workflow">Courier 模板 ID<input id="target-workflow" required placeholder="例如 ABCD1234"><span class="help">从 Courier Dashboard 的消息模板中复制。</span></label><label for="target-routing">指定渠道（可选）<input id="target-routing" placeholder="留空则使用 Courier 中的配置"><span class="help">通常无需填写；多个渠道用英文逗号分隔。</span></label><div class="actions"><button id="create-action" class="btn" type="button">生成发送预览</button></div></div><div class="action-history"><h4>发送记录</h4><div class="delivery-list">'+history+'</div></div></div>'}
async function show(id){const selectionChanged=selected!==id;selected=id;renderJobs();const job=await api('/api/v1/recordings/'+encodeURIComponent(id));const time=recordingTime(job),timeSource=time.label||'设备录音时间';document.querySelector('#workspace-title').textContent=job.scenario_result?.title||'待整理录音';document.querySelector('#workspace-sub').textContent=timeSource+' '+formatDate(time.value)+' · '+scenarioTitle(job.scenario_id)+' · 录音 '+job.recording_id;const currentAudio=document.querySelector('#audio'),resumePlayback=!selectionChanged&&Boolean(currentAudio&&!currentAudio.paused);if(currentAudio&&Number.isFinite(currentAudio.currentTime))playbackPositions.set(job.id,currentAudio.currentTime);document.querySelector('#workspace').innerHTML=pipeline(job)+'<section id="stage-source" class="stage"><div class="stage-label"><div class="stage-number">01</div><h3>来源与转写</h3><div class="small muted">Platform 仅提供已授权录音来源</div></div><div class="stage-body"><div class="meta-grid"><div>状态<b>'+status(job.state)+'</b></div><div>录音时间<b>'+esc(formatDate(time.value))+'</b><span class="small muted">'+esc(timeSource)+'</span></div><div>转写版本<b>'+job.transcript_revision+'</b></div><div>结果数量<b>'+job.artifacts.length+'</b></div></div>'+transcriptStage(job)+'</div></section><section id="stage-process" class="stage"><div class="stage-label"><div class="stage-number">02</div><h3>处理结果</h3><div class="small muted">从转写中提炼标题、要点、决定和行动项</div></div><div class="stage-body">'+summaryStage(job)+'</div></section><section id="stage-scenario" class="stage"><div class="stage-label"><div class="stage-number">03</div><h3>场景与审核</h3><div class="small muted">选择用途，检查结果并确认</div></div><div class="stage-body">'+scenarioStage(job)+'</div></section><section id="stage-action" class="stage"><div class="stage-label"><div class="stage-number">05</div><h3>发送动作</h3><div class="small muted">通过 Courier 预览并发送已确认结果</div></div><div class="stage-body">'+actionStage(job)+'</div></section>';if(selectionChanged)document.querySelector('.workspace').scrollTop=0;bindJob(job,resumePlayback);renderedDetailKey=detailKey(jobs.find(item=>item.id===job.id)||job)}
function bindJob(job,resumePlayback=false){document.querySelectorAll('textarea').forEach(node=>node.oninput=()=>{editing=true});document.querySelectorAll('details.advanced').forEach(node=>node.ontoggle=()=>{editing=node.open});const audio=document.querySelector('#audio');if(audio){const rememberPosition=()=>{if(Number.isFinite(audio.currentTime))playbackPositions.set(job.id,audio.currentTime)};const restorePosition=()=>{const saved=playbackPositions.get(job.id);if(Number.isFinite(saved)&&saved>0)audio.currentTime=saved;if(resumePlayback)void audio.play()};const playSegment=node=>{audio.currentTime=Number(node.dataset.seek)/1000;rememberPosition();void audio.play()};if(audio.readyState>=1)restorePosition();else audio.addEventListener('loadedmetadata',restorePosition,{once:true});audio.onplay=()=>{playing=true};audio.onpause=()=>{playing=false;rememberPosition()};audio.onended=()=>{playing=false;rememberPosition()};audio.ontimeupdate=()=>{rememberPosition();document.querySelectorAll('.segment').forEach(node=>node.classList.toggle('active',audio.currentTime*1000>=Number(node.dataset.start)&&audio.currentTime*1000<Number(node.dataset.end)))};document.querySelectorAll('.segment-time[data-seek]').forEach(node=>node.onclick=event=>{event.stopPropagation();playSegment(node)});document.querySelectorAll('.segment[data-seek]').forEach(node=>node.onclick=()=>playSegment(node))}const saveTranscript=document.querySelector('#save-transcript');if(saveTranscript)saveTranscript.onclick=async()=>run(async()=>{const manual=document.querySelector('#manual-transcript');let transcript;if(manual){const text=manual.value.trim();if(!text)throw new Error('请先输入转写内容');const duration=Number(job.transcript?.duration_ms??job.recording.media?.duration_ms??job.recording.timing?.duration_ms??0);const safeDuration=Number.isSafeInteger(duration)&&duration>=0?duration:0;transcript={...(job.transcript||{schema_version:'demo.transcript.v1',recording_id:job.recording_id,language:'zh',duration_ms:safeDuration,processor:{provider:'manual',model:'studio-editor',version:'1'}}),text,segments:[{id:'manual-0001',start_ms:0,end_ms:safeDuration,text,speaker:null,confidence:null}]}}else{const segments=job.transcript.segments.map((item,index)=>({...item,text:document.querySelector('[data-index="'+index+'"]').value}));transcript={...job.transcript,text:segments.map(item=>item.text).join(' '),segments}}await api('/api/v1/recordings/'+encodeURIComponent(job.id)+'/transcript',{method:'PATCH',headers:{'content-type':'application/json','if-match':'"transcript-'+job.transcript_revision+'"'},body:JSON.stringify({transcript,expected_revision:job.transcript_revision,editor:'studio-ui'})});editing=false},'Transcript Revision 已保存');const generate=document.querySelector('#generate-summary');if(generate)generate.onclick=async()=>{if(summaryRequests.has(job.id))return;summaryRequests.add(job.id);summaryRequestErrors.delete(job.id);generate.disabled=true;generate.setAttribute('aria-busy','true');generate.textContent='处理中…';notify('处理任务已启动，本地模型正在生成结果，请稍候');try{const result=await api('/api/v1/recordings/'+encodeURIComponent(job.id)+'/summary/generate',{method:'POST'});if(result?.summary_state==='failed')throw new Error(result.summary_error_message||result.summary_error_code||'处理失败');notify('Processor 与 Scenario 已重新生成')}catch(error){const message=error instanceof Error?error.message:String(error);summaryRequestErrors.set(job.id,message);notify(message,true)}finally{summaryRequests.delete(job.id);await load().catch(error=>notify(error.message,true))}};const saveSummary=document.querySelector('#save-summary');if(saveSummary)saveSummary.onclick=()=>run(()=>api('/api/v1/recordings/'+encodeURIComponent(job.id)+'/summary',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({summary:JSON.parse(document.querySelector('#summary-json').value),expected_revision:job.summary_revision,editor:'studio-ui'})}),'Processor Artifact 已修订');const selector=document.querySelector('#scenario-select');if(selector)selector.onchange=()=>run(()=>api('/api/v1/recordings/'+encodeURIComponent(job.id)+'/scenario',{method:'PUT',headers:{'content-type':'application/json'},body:JSON.stringify({scenario_id:selector.value})}),'场景包已切换并重新投影');const saveScenario=document.querySelector('#save-scenario');if(saveScenario)saveScenario.onclick=()=>run(()=>api('/api/v1/recordings/'+encodeURIComponent(job.id)+'/scenario',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({result:JSON.parse(document.querySelector('#scenario-json').value),expected_revision:job.scenario_revision,editor:'studio-ui'})}),'Scenario Revision 已保存');const confirmScenario=document.querySelector('#confirm-scenario');if(confirmScenario)confirmScenario.onclick=()=>run(()=>api('/api/v1/recordings/'+encodeURIComponent(job.id)+'/scenario/confirm',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({actor:document.querySelector('#review-actor').value||'studio-ui',note:document.querySelector('#review-note').value||null})}),'Scenario Revision 已确认');const unconfirmScenario=document.querySelector('#unconfirm-scenario');if(unconfirmScenario)unconfirmScenario.onclick=()=>run(()=>api('/api/v1/recordings/'+encodeURIComponent(job.id)+'/scenario/unconfirm',{method:'POST'}),'Scenario 确认已撤销');const createAction=document.querySelector('#create-action');if(createAction)createAction.onclick=()=>run(()=>api('/api/v1/recordings/'+encodeURIComponent(job.id)+'/actions',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({target:{id:'ui-target',name:document.querySelector('#target-name').value,recipient:{kind:'user',user_id:document.querySelector('#target-user').value},workflow_id:document.querySelector('#target-workflow').value,routing:document.querySelector('#target-routing').value.split(',').map(value=>value.trim()).filter(Boolean),version:1}})}),'动作预览已生成，尚未发送');document.querySelectorAll('.execute-action').forEach(node=>node.onclick=()=>run(()=>api('/api/v1/recordings/'+encodeURIComponent(job.id)+'/actions/'+encodeURIComponent(node.dataset.id)+'/execute',{method:'POST'}),'动作已提交给 Courier'));document.querySelectorAll('.refresh-delivery').forEach(node=>node.onclick=()=>run(()=>api('/api/v1/recordings/'+encodeURIComponent(job.id)+'/deliveries/'+encodeURIComponent(node.dataset.id)+'/refresh',{method:'POST'}),'Provider 状态已同步'));document.querySelectorAll('.cancel-delivery').forEach(node=>node.onclick=()=>run(()=>api('/api/v1/recordings/'+encodeURIComponent(job.id)+'/deliveries/'+encodeURIComponent(node.dataset.id)+'/cancel',{method:'POST'}),'Delivery 已取消'))}
async function run(action,message){try{await action();editing=false;notify(message);await load({forceDetail:true})}catch(error){notify(error.message,true)}}
function renderSetup(){selected=null;document.querySelector('#workspace-title').textContent='设置与诊断';document.querySelector('#workspace-sub').textContent='External / Local Full';document.querySelector('#workspace').innerHTML='<div class="setup"><div class="barrier">生产运行只提供 External 和 Local Full。Local Full 的模型处理不访问公网，通知外联默认关闭。</div><form id="setup-form"><section><div class="kicker">01 设备平台</div><div class="setup-grid"><label class="wide">平台地址<input name="platform_url" required placeholder="https://device.example.com"></label><label>应用令牌<input name="application_token" type="password" required autocomplete="new-password"></label><label>Webhook 密钥<input name="webhook_secret" type="password" required autocomplete="new-password"></label><label>下一个 Webhook 密钥<input name="webhook_secret_next" type="password" autocomplete="new-password"></label></div></section><section><div class="kicker">02 外部处理服务</div><div class="setup-grid"><input type="hidden" name="processor_kind" value="http"><label>语音识别服务地址<input name="processor_endpoint" value="http://127.0.0.1:9001" required></label><label>语音识别 API 密钥<input name="processor_api_key" type="password"></label><label>摘要服务地址<input name="summary_endpoint" value="http://127.0.0.1:9002" required></label><label>摘要服务 API 密钥<input name="summary_api_key" type="password"></label><label>摘要模型<input name="summary_model" value="configured-model"></label><label>提示词版本<input name="summary_prompt_version" value="meeting-v1"></label></div></section><section><div class="kicker">03 消息服务</div><div class="setup-grid"><label class="toggle"><input name="notification_enabled" type="checkbox" value="true">显式启用 Courier 消息外发</label><label>Courier API 密钥<input name="courier_api_key" type="password"></label><label>Courier 服务地址<input name="courier_base_url" value="https://api.courier.com"></label><label class="wide">Studio 公开地址<input name="studio_public_url" placeholder="https://studio.example.com"></label></div></section><section><div class="setup-grid"><label>结果保留天数<input name="retention_days" type="number" value="30" min="1"></label></div><div class="actions"><button class="btn" type="submit">验证并原子保存</button><button id="doctor" class="btn secondary" type="button">运行诊断</button></div></section></form><pre id="doctor-result" class="editor mono small"></pre></div>';document.querySelector('#setup-form').onsubmit=event=>{event.preventDefault();const data=Object.fromEntries(new FormData(event.target));data.notification_enabled=data.notification_enabled==='true';run(()=>api('/api/config',{method:'PUT',headers:{'content-type':'application/json'},body:JSON.stringify(data)}),'设置完成')};document.querySelector('#doctor').onclick=async()=>{try{document.querySelector('#doctor-result').textContent=JSON.stringify(await api('/api/v1/doctor'),null,2)}catch(error){document.querySelector('#doctor-result').textContent=error.message}}}
document.querySelector('#search').oninput=renderJobs;document.querySelector('#filter').onchange=renderJobs;document.querySelector('#sync').onclick=()=>run(()=>api('/api/v1/recordings/sync',{method:'POST'}),'授权来源已刷新');document.querySelector('#setup-open').onclick=renderSetup;document.querySelector('#token').onclick=()=>{document.querySelector('#token-panel').classList.toggle('hidden');document.querySelector('#token-value').value=sessionStorage.getItem('studio.operatorToken')||''};document.querySelector('#token-save').onclick=()=>{const value=document.querySelector('#token-value').value;if(value)sessionStorage.setItem('studio.operatorToken',value);else sessionStorage.removeItem('studio.operatorToken');document.querySelector('#token-panel').classList.add('hidden');notify('操作令牌已保存到当前会话')};document.querySelector('#token-clear').onclick=()=>{sessionStorage.removeItem('studio.operatorToken');document.querySelector('#token-value').value='';document.querySelector('#token-panel').classList.add('hidden');notify('操作令牌已清除')};
load().catch(error=>notify(error.message,true));timer=setInterval(()=>{if(!editing&&!playing)load().catch(()=>{})},4000);
</script>
</body>
</html>`;
